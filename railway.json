{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "preDeployCommand": "pnpm prisma migrate deploy --skip-generate",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 60
  }
}
